initSidebarItems({"fn":[["announce_uptime","Periodically send a message on the given `topic` ([`tokio::sync::watch::Sender`]). The message will be `\"Up for Xs\"`, where `X` increases in steps given in `duration`. The interval period is given by `duration`."],["handle_connection","Monitor the `reader`, `rx` and `topic_rx` for messages. When receiving bytes on `reader`, forward them on the [`tokio::sync::broadcast::Sender`]. When receiving a message on `rx`, where the source socket address is not our own, forward it on `writer` (else, discard it). When the content of `topic_rx` changed, fetch it, then format and forward it on `writer`."]]});